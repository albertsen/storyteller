.meta
  =format_date(item.created_at)
  &nbsp;&mdash;&nbsp;
  =link_to number_of_comments(item.comments.size), deep_item_url(item, :anchor => "comments")
  &nbsp;&mdash;&nbsp;
  =link_to "Link", deep_item_url(item)
  -if logged_in?
    &nbsp;&mdash;&nbsp;
    =link_to "Edit", edit_item_url(item)